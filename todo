Do a live test to get real response when user cancels paypal_authable flow and replace code in devise_paypal
Rename supplier order to product order and create a new supplier order model to hold product orders in a supplier order
Allow seller to complete their own supplier orders
Allow user to verify number by simply texting their name
Fix UTF-8 incoming and outgoing messages with Tropo
Email notifications
Test locally with postgreSQL
Change valid message commands to have more logic (i.e. only display command for verifying mobile number if mobile number is not verified etc)
Change default behaviour of mn to be verify & change default action to be verify if the number is not verified
Modify validation for devise (allow suppliers to be created with just a name)
Make some rudamentary view files for adding products and suppliers (DO ONLY WHAT WE NEED)
Live testing

